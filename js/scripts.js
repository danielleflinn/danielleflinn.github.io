// Everything OK?
console.log('OK');

// Light Gallery

$('.gallery-1').lightGallery({
    selector: 'a',
    thumbnail: false,
    download: false,
    autoplay: false,
    share: false,
    autoplayControls: false,
    mode: 'lg-scale-up'
});
